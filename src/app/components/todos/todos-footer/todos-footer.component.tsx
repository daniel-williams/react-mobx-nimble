import * as React from 'react';

import { EmptyProps, EmptyState } from '../../../models';

import * as styles from './todos-footer.component.scss';


export class TodosFooter extends React.Component<EmptyProps, EmptyState> {
  render() {
    return (
      <div>footer</div>
    );
  }
}
